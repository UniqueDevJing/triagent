# 智能医疗系统 · Agent 功能验证报告

- 生成时间：2026-09-04 19:5x（GMT+8）
- 验证方式：对运行中的后端（8080）执行真实接口调用（DeepSeek 已充值），SSE 事件逐条解析断言；前端核对生产构建产物。
- 结论：**预期功能全部实现并通过验证**，仅两处「按设计拦截」的行为需知悉（重复确认被拒 / 红旗安全从严），均非缺陷。

## 1. 鉴权与安全基线

| 预期 | 结果 | 证据 |
|---|---|---|
| SSE 端点需登录 | ✅ | 无 token `401`；伪造 token `401` |
| CORS/登录态走 Sa-Token | ✅ | 登录后全部接口 200 |

> 说明：本环境验证码开启，自动化登录通过 `captchaImage → Redis 取码 → login` 完成（管理员 admin/admin123）。

## 2. 智能分诊核心链路（Phase 1/3）

| 预期 | 结果 | 证据 |
|---|---|---|
| 红旗症状确定性短路（不依赖 LLM） | ✅ | 「胸痛+呼吸困难」→ `agent=分诊顾问`，`urgency=EMERGENCY`，科室 `急诊科/心内科/呼吸内科`，conf 0.9，来源 5 条，免责声明齐全，正文含 120/急诊指引 |
| 输出护栏补免责 | ✅ | EMERGENCY 回答含 disclaimer 字段与正文提示 |
| 多轮澄清状态机（部位/时长/伴随） | ✅ | 「不太舒服」→ `clarify` 追问 bodyPart；补齐「头痛/3天/恶心」后进入 LLM 分诊输出 `URGENT` |
| 结构化分诊 JSON（schema 固化） | ✅ | done.triage 完整：urgency/departments/confidence/followUp/disclaimer（BeanOutputConverter JSON Schema 已进提示词） |
| JSON 解析失败自动重试（G8 兜底） | ✅ | 日志曾出现「JSON 重试成功（首轮解析失败）」；重试后输出稳定 100% |

## 3. 多 Agent 与工具链（Phase 2）

| 预期 | 结果 | 证据 |
|---|---|---|
| 路由到报告解读 Agent + 真实工具调用 | ✅ | 「解读孙明伟体检报告」→ `agent=报告解读`，事件含 `searchMembers → getMemberAssessments`，回答 1812 字、来源 2 条 |
| 预约管家两步确认 + 幂等 | ✅ | 相同参数两次（不同会话）：`createPreOrder` 均返回**预订单 #5 [PENDING]**（跨会话幂等键生效，仅 1 行落库） |
| 预订单确认端点 | ✅ | `POST /preorders/5/confirm` → 200，状态 PENDING→**CONFIRMED**；再次确认被业务规则拒绝（500「已确认」）——防重复确认，属设计行为 |
| 会话内意图连贯（连续性路由） | ✅ | 预约流程中用户答「症状是胸痛」→ 后端日志「字面命中 TRIAGE，沿用会话 Agent SCHEDULER」；模型给出急症提醒后**继续预约流程**而非转急救；最终正常生成预订单（主诉=胸痛） |

## 4. RAG 溯源（Phase 3）

| 预期 | 结果 | 证据 |
|---|---|---|
| 知识检索注入 + 来源回传 | ✅ | EMERGENCY/分诊/报告场景 `done.sources` 均非空（疾病库+科普文章），`done` 载荷含 source/refId/title/score |
| 输入护栏（指令注入/空消息） | ✅ | 编排层 Advisor 链（日志→输入护栏→RAG→输出护栏→审计）编译并运行 |

## 5. 可观测性（Phase 4）

| 预期 | 结果 | 证据 |
|---|---|---|
| 指标采集与查询 | ✅ | `GET /api/v1/assistant/metrics`：total 6（本轮窗口）、toolCalls=11=results（1:1）、byAgent TRIAGE3/REPORT1/SCHEDULER2、byTurnType EMERGENCY1/CLARIFY1/LLM4、avg 4021ms |
| 透明推理 SSE 事件 | ✅ | `token / tool_call / tool_result / clarify / done / error` 全事件在真实场景中逐一出现 |

## 6. 前端（health-web）

| 预期 | 结果 | 证据 |
|---|---|---|
| 智能分诊透明推理页（含信息栏重构） | ✅ | `npm run build` 通过；生产包 `AssistantChat-*.js` 含「会话上下文」等新版标记 |
| 澄清快捷回复 / 进度面板 / 会话上下文栏 | ✅ | 源码 + dist 已含（澄清 chips、部位/时长/伴随进度、Agent 标签、清空会话） |
| 侧边栏入口 | ✅ | App.vue 菜单「智能分诊」；路由 `/assistant` 注册 |
| 旧 Coze/AI 残留清理 | ✅ | index.html 挂件、旧页/API、后端 agent/intelligent 控制器、seed 与表已全清；`grep` 全仓库无残余 |

## 7. 评估（量化）

| 指标 | 值 |
|---|---|
| G1–G10 golden set（docs/agent/评估报告.md v2） | 紧急度 0.90 / 科室命中 1.00 / 综合 9.4/10 |
| 红旗样例安全通过 | 4/4 |

## 已知行为（非缺陷，需知悉）

1. 重复确认已 CONFIRMED 的预订单返回业务错误——防重复，前端应提示「已确认」而非报错。
2. G4（呼吸困难）与 G8（内分泌相关）实际判得**比 golden 期望更紧一档**（安全从严），符合设计取向。
3. 登录开启图形验证码——对接方需走 `captchaImage` 流程（与旧版行为一致，非本次回归）。
