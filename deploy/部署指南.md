# 传智健康管理系统 - 部署指南

## 服务器要求

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 1核 | 2核 |
| 内存 | 2GB | 4GB |
| 系统 | Ubuntu 20.04+ / CentOS 7+ |
| 磁盘 | 20GB | 40GB+ |

> 2GB 内存只能跑 Docker 方式。1GB 服务器只能用外部 MySQL+Redis，或换 H2 内嵌数据库。

---

## 方式一：Docker Compose（推荐，2GB+ 服务器）

### 1. 上传文件

将整个 `deploy/` 目录上传到服务器：

```bash
# 在本地执行（替换为你的服务器IP）
scp -r deploy/ root@你的服务器IP:/opt/health/
```

### 2. 配置密钥

```bash
ssh root@你的服务器IP
cd /opt/health/deploy/

# 创建 .env
cp .env.example .env
nano .env   # 填入真实密码和密钥
```

### 3. 一键启动

```bash
chmod +x deploy.sh
./deploy.sh
```

### 4. 访问

```
http://服务器IP          # 前端页面
http://服务器IP:8080     # API (Knife4j文档)
```

---

## 方式二：手动部署（1GB 服务器）

### 1. 安装依赖

```bash
# MySQL (或使用云数据库)
apt-get install -y mysql-server
# Redis
apt-get install -y redis-server
# Java 17
apt-get install -y openjdk-17-jre-headless
# Nginx
apt-get install -y nginx
```

### 2. 初始化数据库

```bash
mysql -u root -p < init.sql
```

### 3. 启动后端

```bash
# 限制内存
java -Xms64m -Xmx256m -jar health-server-v3.jar \
  --spring.profiles.active=prod \
  --DB_PASSWORD=你的密码 \
  --JWT_SECRET=你的JWT密钥 \
  --AI_API_KEY=你的AI密钥 \
  --REDIS_HOST=localhost \
  --DB_HOST=localhost &
```

### 4. 配置 Nginx

```bash
cp nginx.conf /etc/nginx/conf.d/health.conf
# 修改 proxy_pass 中的 backend 为 localhost
sed -i 's/backend/localhost/g' /etc/nginx/conf.d/health.conf
systemctl reload nginx
cp -r dist/* /usr/share/nginx/html/
```

---

## 常用命令

```bash
# Docker
docker compose ps            # 查看状态
docker compose logs -f       # 查看日志
docker compose restart       # 重启
docker compose down          # 停止

# 手动
ps aux | grep java           # 查看Java进程
tail -f logs/health.log      # 查看日志
```

---

## 默认账号

| 账号 | 密码 |
|------|------|
| admin | admin123 |

> 首次登录后请立即修改密码！

## 安全清单

- [ ] 修改数据库密码
- [ ] 修改 JWT 密钥为随机字符串
- [ ] 设置 AI_API_KEY
- [ ] 配置 HTTPS 证书
- [ ] 修改 admin 默认密码
- [ ] 配置防火墙（只开放 80/443 端口）
